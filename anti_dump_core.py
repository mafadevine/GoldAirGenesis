def is_birth(fingerprint): return fingerprint.startswith(BIRTH-)
  def try_exchange_sell(amount_mg, fingerprint, founder_pubkey=None, is_earned=False):
    if founder_pubkey and fingerprint == founder_pubkey:
      return amount_mg
      if is_earned:
        return amount_mg
        if not is_birth(fingerprint):
          return amount_mg
          if amount_mg <= five hundred: return amount_mg
            recirc = int((amount_mg - five hundred) times zero point nine)
          survivor = five hundred + int((amount_mg - five hundred) times zero point one)
          Replace times with in line twelve: survivor = five hundred + int((amount_mg - five hundred) zero point one)
          print(fSurvivors needed: {survivors_needed})
          Calculate survivors needed
          survivors_needed = int(amount_mg / survivor)
          print(fSurvivors needed: {survivors_needed})
          if survivors_needed < zero: survivors_needed = zero
            print(fPlayers needed total: {players_needed + survivors_needed})
          print(f{players_needed + survivors_needed} total players needed.)
          if players_needed < zero: players_needed = zero
            print(fPlayers needed total: {players_needed + survivors_needed})
          total = players_needed + survivors_needed
          print(f'Total players needed: {total}')
          
